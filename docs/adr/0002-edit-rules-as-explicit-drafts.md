# 规则编辑采用显式草稿事务

新建和修改逻辑规则都先进入规则草稿，只有用户应用后才写入正式问卷；取消或放弃草稿不会改变问卷，也不会产生未保存状态。我们接受多一次“应用”操作，以换取一致的撤销语义、可靠校验，以及表单式编辑和未来可视化编辑共享同一创作流程。

## Consequences

- 规则草稿的变化只进入临时预览，不参与正式问卷保存与发布。
- 切换规则或离开逻辑模式时，未应用的变化必须先确认放弃。
- 规则的默认值、约束、自动命名、校验与提交集中在同一模块。
