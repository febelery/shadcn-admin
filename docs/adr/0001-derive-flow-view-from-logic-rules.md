# 流程视图由逻辑规则派生

逻辑规则和问卷结构是事实来源，流程视图由二者生成，不保存一份可独立变化的流程业务状态。未来的可视化编辑通过拖线创建规则草稿，并在确认后提交为逻辑规则；这样既保留图形化编辑能力，也避免问卷行为与图结构产生两套互相同步的事实来源。

## Consequences

- 主流程投影只呈现已确认的逻辑规则；规则草稿以临时预览叠加。
- 只有题目顺序或规则关系等拓扑变化触发重新布局。
- 表单式规则编辑和未来可视化规则编辑作为两个适配器，共享同一规则变更实现。
